version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: swos_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: swos
      POSTGRES_USER: swos_user
      POSTGRES_PASSWORD: swos_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  postgres_data: